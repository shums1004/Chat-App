export const host = "http://localhost:3000";

export const registerApi = `${host}/register`;

export const loginApi = `${host}/login`; 

export const logoutApi = `${host}/logout`;

export const setAvatarApi = `${host}/setavatar`; 

export const allUsersApi = `${host}/allusers`

export const getUserApi = `${host}/getUser`;

export const sendMessageApi = `${host}/messages/addmessage`

export const getMessageApi = `${host}/messages/getmessage`